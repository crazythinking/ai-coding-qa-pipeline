<?xml version="1.0" encoding="UTF-8"?>
<!-- ai-coding-qa-pipeline jacoco + pitest pom 片段(由 pipeline-setup 引导生成,B 类)。
     合并进项目 pom.xml 的 <build><plugins> 下;替换 <module> 为项目坐标。 -->
<build>
  <plugins>
    <!-- 覆盖率: mvn -q org.jacoco:jacoco-maven-plugin:check -->
    <plugin>
      <groupId>org.jacoco</groupId>
      <artifactId>jacoco-maven-plugin</artifactId>
      <version>0.8.12</version>
      <executions>
        <execution>
          <id>prepare-agent</id>
          <goals><goal>prepare-agent</goal></goals>
        </execution>
        <execution>
          <id>report</id>
          <goals><goal>report</goal></goals>
        </execution>
        <execution>
          <id>check</id>
          <goals><goal>check</goal></goals>
          <configuration>
            <rules>
              <rule>
                <element>BUNDLE</element>
                <limits>
                  <limit><counter>LINE</counter><value>COVEREDRATIO</value><minimum>1.00</minimum></limit>
                </limits>
              </rule>
            </rules>
          </configuration>
        </execution>
      </executions>
    </plugin>
    <!-- 变异: mvn -q org.pitest:pitest-maven:mutationCoverage -DwithHistory -->
    <plugin>
      <groupId>org.pitest</groupId>
      <artifactId>pitest-maven</artifactId>
      <version>1.15.0</version>
      <configuration>
        <targetClasses><param><module>.*</param></targetClasses>
        <targetTests><param><module>.*Test</param></targetTests>
        <mutators><mutator>DEFAULTS</mutator></mutators>
      </configuration>
    </plugin>
  </plugins>
</build>